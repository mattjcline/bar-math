# Auth

Detail doc for sign-in, RLS policies (`supabase/policies.sql`), and the invite/claim flow. Read this before touching any of that — see `CLAUDE.md` for the one-paragraph summary and everything else about the project.

Two separate, deliberately different auth models:
- **Calculator**: a single shared password (`REACT_APP_SITE_PASSWORD`), checked client-side, unlock flag in `localStorage`. Not real security (the password is inlined in the shipped JS bundle) — it only exists to keep casual visitors from stumbling onto it. No per-person identity.
- **Admin** (`Admin.tsx`): real Supabase Auth via a one-time email code (`signInWithOtp` to request it, `verifyOtp({ email, token, type: "email" })` to redeem it — no magic link, no redirect). This used to be a clickable magic link, but iOS routes any link tapped from Mail into Safari, never into the installed home-screen PWA, and Safari/standalone-PWA storage aren't shared — so a manager signing in from the installed app would end up authenticated in Safari instead. Typing the code in-place sidesteps the whole problem, since the user never leaves the app. `public.users.auth_user_id` links a Supabase Auth account to a row in the app's own `users` table; access is authorized only if that row's `role` is `'admin'` or `'manager'` *and* `is_active` is true. Regular auto-created bartenders never get an `auth_user_id` and can't sign in anywhere. The email template (Supabase dashboard → Authentication → Email Templates → Magic Link, must include `{{ .Token }}`) requires custom SMTP on the free tier — Supabase disabled template editing on its own default sender for new free-tier projects; this project uses Resend (verified sending domain required, or sending 500s with an unhelpful error).
- **Onboarding** an admin/manager is invite-then-self-link: an existing admin/manager invites someone from Staff's "Admins & Managers" section (`AdminUsers.tsx`), which creates a `users` row with `role`/`email` set but `auth_user_id` still null. The first time that person signs in with that email, `Admin.tsx`'s `evaluateSession` finds no row linked to their new `auth_user_id`, falls back to looking up an unclaimed row by matching `email`, and if found, claims it (`update users set auth_user_id = ...`) right there — no separate confirmation step, they land straight in the admin panel. This *must* stay narrowly scoped: the RLS policy that allows this (`claim invited user row`) only matches rows with `auth_user_id is null` and an exact (case-insensitive) email match against the signed-in user's own verified JWT, and only allows setting `auth_user_id` to their own id — see "Auth model" below for why the insert side needed tightening too.
- RLS policies (`supabase/policies.sql`) grant identical *read* access `to anon, authenticated` — signing into the admin panel must not change what the calculator itself can see in that browser session. *Writes* are a deliberate exception: each admin-panel feature that mutates something sensitive (voiding a report, editing `bars`, editing/deactivating a `users` row, unlinking `user_bars`) gets its own policy gated to `role in ('admin', 'manager')`, added alongside that feature as it's built — not preemptively, and not extended to reads. These policies check role only, not which specific row is targeted: once authenticated as admin/manager you're a fully trusted operator of the table, the same trust model repeated across `update bars`, `update users`, `void reports`, and `unlink user from bar`. One exception to "role only, not row-scoped": `create users` used to be a single wide-open `with check (true)` insert policy (needed so the unauthenticated calculator can auto-create bartender rows) — that became a real privilege-escalation path once self-claim existed, since anyone could otherwise insert their own `role: 'admin'` row and then sign in to claim it. It's now split into `create bartender` (still wide open to anon/authenticated, but `with check (role = 'bartender')`) and `invite admin or manager` (authenticated + already-admin/manager only). Migrations to `policies.sql` aren't applied automatically — after editing the file, the corresponding SQL has to be run by hand in the Supabase SQL editor (there's no CLI link/migration runner set up for this project).
